@model Cart
@{
	ViewData["Title"] = "UserCart";
}
<h1>Empty</h1>
<div>
	@if (Model != null && Model.CartDetail != null && Model.CartDetail.Count > 0)
	{
		<h3>My Cart</h3>
		<table>

		</table>
	}
</div>